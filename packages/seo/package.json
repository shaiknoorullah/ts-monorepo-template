{
  "name": "@pkg/seo",
  "version": "0.0.0",
  "description": "SEO helpers — OpenGraph, JSON-LD (schema.org), sitemap, robots.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./jsonld": "./src/jsonld.ts"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "type-check": "tsc -b --noEmit"
  },
  "dependencies": {
    "schema-dts": "^1.1.5"
  },
  "peerDependencies": {
    "@resvg/resvg-wasm": "^2.6.0",
    "satori": "^0.12.0"
  },
  "peerDependenciesMeta": {
    "@resvg/resvg-wasm": { "optional": true },
    "satori": { "optional": true }
  },
  "devDependencies": {
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
