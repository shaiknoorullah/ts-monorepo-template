[package]
name = "contracts"
version = "0.0.0"
edition = "2021"
publish = false
description = "Rust bindings for ts-monorepo-template/contracts (generated via buf)."
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
prost = "0.13"
prost-types = "0.13"
tonic = "0.12"
