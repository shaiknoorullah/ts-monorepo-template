{
  "name": "@pkg/api-client",
  "version": "0.0.0",
  "description": "Typed fetch wrapper. Carries auth + tenancy headers. Types generated from the api-gateway OpenAPI spec.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "type-check": "tsc -b"
  },
  "dependencies": {
    "@pkg/auth-client": "workspace:*",
    "@pkg/tenancy-client": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
