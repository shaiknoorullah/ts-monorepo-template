# internal/cli/src/core/__tests__/fixtures/secretspec-ok.toml
[project]
name = "fixture"
revision = 1

[profiles.default]
backend = "keyring"
fallback = "env"

[secrets.DATABASE_URL]
required = true
profiles = ["p-solo", "p-hobby", "p-startup-small"]
default_dev = "postgres://app:app@localhost:5432/app"

[secrets.OPENAI_API_KEY]
required = false
profiles = ["p-startup-small"]

[secrets.GHCR_TOKEN]
required = true
profiles = ["ci"]
ci_only = true
