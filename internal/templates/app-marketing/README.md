# {{scope}}/{{name}}

Astro 5 marketing / landing pages. Deploys to Cloudflare Pages.

```bash
pnpm --filter {{scope}}/{{name}} dev
pnpm --filter {{scope}}/{{name}} build
pnpm --filter {{scope}}/{{name}} exec wrangler pages deploy dist
```

See `docs/specs/frontend/marketing-and-landing-pages.md`.
