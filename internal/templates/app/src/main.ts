// {{scope}}/{{name}} — entry point

async function main(): Promise<void> {
  // eslint-disable-next-line no-console
  console.log('{{name}} starting...')
  // AGENT-TODO({{name}}): wire @pkg/config + @pkg/logger and your framework of choice.
}

void main()
