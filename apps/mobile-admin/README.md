# @app/mobile-admin

Admin-facing mobile app. Expo + expo-router. iOS + Android.

```bash
pnpm --filter @app/mobile-admin ios
eas build --platform all --profile production
```

See `docs/specs/frontend/mobile-and-cross-platform.md`.
