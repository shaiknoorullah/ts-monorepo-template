# Per-app container build flags (spec Section 5.5 + 5.10).
# Read by infra/build/container.sh and the GHA matrix workflow.
language: go
template: internal/templates/dockerfiles/go.Dockerfile.template
binary: server
platforms:
  - linux/amd64
# Uncomment to opt in to arm64 (doubles GHA build time via QEMU):
# - linux/arm64
