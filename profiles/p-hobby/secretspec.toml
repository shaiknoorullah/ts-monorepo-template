[profile]
id = "p-hobby"

[backend]
backend = "keyring"

[secrets.required]
GITHUB_TOKEN = "GitHub PAT for ghcr.io"
CLOUDFLARE_API_TOKEN = "Cloudflare DNS + edge"

[secrets.optional]
SLACK_WEBHOOK_URL = "ArgoCD notifications"
