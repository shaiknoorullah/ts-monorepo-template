---
galaxy_info:
  role_name: container_runtime
  author: platform-infra
  description: 'Install containerd or cri-o based on container_manager group_var'
  license: Apache-2.0
  min_ansible_version: '2.16'
dependencies:
  - role: common
