---
galaxy_info:
  role_name: observability_agents
  author: platform-infra
  description: 'node-exporter + promtail + otel-collector (host fallback when cluster bringup pre-Argo)'
  license: Apache-2.0
  min_ansible_version: '2.16'
dependencies: []
