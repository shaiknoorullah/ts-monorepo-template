apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: apps-staging
  namespace: argocd
  labels:
    argocd.argoproj.io/self-heal: 'true'
  annotations:
    argocd.argoproj.io/sync-wave: '-80'
spec:
  description: Per-env app project for env=staging
  sourceRepos:
    - https://github.com/REPLACE_ORG/REPLACE_REPO.git
  destinations:
    - namespace: '*-staging'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist: []
  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'
  orphanedResources:
    warn: true
