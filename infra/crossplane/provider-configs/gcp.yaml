apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: provider-gcp-creds
  namespace: crossplane-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: platform-store
    kind: ClusterSecretStore
  target:
    name: provider-gcp-creds
    creationPolicy: Owner
  data:
    - secretKey: credentials
      remoteRef:
        key: crossplane/gcp-sa-json
---
apiVersion: gcp.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  projectID: ''
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: provider-gcp-creds
      key: credentials
