apiVersion: pn.cloud/v1alpha1
kind: KeycloakClientClaim
metadata:
  name: py-hello
  namespace: staging
  labels:
    app.kubernetes.io/name: py-hello
    app.kubernetes.io/component: identity
spec:
  env: staging
  realm: platform
  clientId: py-hello-staging
  redirectUris:
    - https://py-hello.staging.pn.cloud/callback
  scopes:
    - openid
    - profile
    - email
  groupMappers:
    - { group: platform-users, claim: groups }
  serviceAccount:
    enabled: false
    roles: []
  connectionSecret: py-hello-oidc-conn
  compositionSelector:
    matchLabels:
      pn.cloud/variant: default
