apiVersion: pn.cloud/v1alpha1
kind: PostgresClusterClaim
metadata:
  name: py-hello
  namespace: staging
  labels:
    app.kubernetes.io/name: py-hello
    app.kubernetes.io/component: postgres
spec:
  env: staging
  size: s
  version: '16'
  databases:
    - { name: py_hello, owner: py_hello_app }
  pooler:
    enabled: true
    replicas: 2
    mode: transaction
  backup:
    enabled: true
    retentionDays: 7
  observability:
    prometheus: true
    dashboard: true
  connectionSecret: py-hello-pg-conn
  compositionSelector:
    matchLabels:
      pn.cloud/variant: singlenode
