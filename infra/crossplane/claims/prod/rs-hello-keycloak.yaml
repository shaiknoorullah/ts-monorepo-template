apiVersion: pn.cloud/v1alpha1
kind: KeycloakClientClaim
metadata:
  name: rs-hello
  namespace: prod
  labels:
    app.kubernetes.io/name: rs-hello
    app.kubernetes.io/component: identity
spec:
  env: prod
  realm: platform
  clientId: rs-hello-prod
  redirectUris:
    - https://rs-hello.pn.cloud/callback
  scopes:
    - openid
    - profile
    - email
  groupMappers:
    - { group: platform-users, claim: groups }
  serviceAccount:
    enabled: false
    roles: []
  connectionSecret: rs-hello-oidc-conn
  compositionSelector:
    matchLabels:
      pn.cloud/variant: default
