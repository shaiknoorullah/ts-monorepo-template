apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition
metadata:
  name: xrecordencryptionfilters.pn.cloud
spec:
  group: pn.cloud
  names: { kind: XRecordEncryptionFilter, plural: xrecordencryptionfilters }
  claimNames: { kind: RecordEncryptionFilterClaim, plural: recordencryptionfilterclaims }
  defaultCompositionRef: { name: xrecordencryptionfilter-kroxylicious }
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [proxyPlane, name, kms, envelopeKeyRef]
              properties:
                proxyPlane: { type: string }
                name: { type: string }
                kms: { type: string, enum: [vault, akv, aws-kms, gcp-kms] }
                envelopeKeyRef: { type: string }
                topics:
                  type: array
                  items: { type: string }
