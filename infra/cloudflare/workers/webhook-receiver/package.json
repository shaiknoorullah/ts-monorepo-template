{
  "name": "@infra/cf-webhook-receiver",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker that receives inbound webhooks and queues them for processing.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "catalog:",
    "wrangler": "^3.80.0"
  }
}
