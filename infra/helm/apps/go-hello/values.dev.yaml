env: dev
workload:
  replicas: 1
autoscaling:
  enabled: false
pdb:
  enabled: false
observability:
  tracing:
    samplerArg: '1.0'
networkPolicy:
  egress:
    external:
      - { host: webhook.site, port: 443 }
