{{- /*
  Rollout rendering lives inside the unified lib-chart.workload define
  in deployment.yaml (single named template, dispatched on .Values.workload.kind).
  This file is retained as a stub for path-stability but contains no
  active template body.
*/ -}}
